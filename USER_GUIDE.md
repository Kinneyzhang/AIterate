# AIIterate 用户使用文档

> 用 AIIterate 学一个东西，不是“问完就走”，而是走完一轮可验证的学习闭环。

AIIterate 的核心目标很简单：帮你确认“我真的懂了”，而不是“我刚才看懂了”。它会把一个问题或观点推进成结构化学习材料，再要求你主动复述、追问、做费曼检验，最后进入复习队列。

## 1. 入口与登录

访问地址：

```text
http://192.168.31.222:7070
```

如果出现登录页，输入 Admin Token。登录成功后浏览器会保存 HttpOnly session cookie，后续刷新不需要重复登录。

顶部常用入口：

- `＋`：新建学习主题。
- `知识地图`：查看知识树、节点掌握度和推荐学习方向。
- `指挥中心`：查看今天最该推进的学习、费曼和复习。
- `设置`：配置模型、联网搜索、数据库、费曼通过分。
- `刷新`：重新拉取当前数据。
- `主题切换`：暗色/亮色主题切换。

建议每天第一次打开时先看“指挥中心”，不要直接随机点历史记录。

## 2. 一次完整学习长什么样

```text
新建主题
  → AI 生成学习材料
  → 阅读材料
  → 写下自己的理解
  → AI 评分并指出薄弱点
  → 继续追问或修正
  → 开始费曼检验
  → 通过：进入复习队列
  → 未通过：回到修正阶段
```

重点不是和 AI 多聊，而是不断把“输入”变成“你能讲出来的理解”。

## 3. 新建学习主题

点击 `＋`，输入一个问题或观点。

### 适合的问题

```text
PostgreSQL 的 MVCC 是怎么避免读写冲突的？
```

```text
Transformer 里的 attention 为什么能捕捉长距离依赖？
```

```text
Linux cgroup v2 和 namespace 分别解决什么问题？
```

### 适合的观点

```text
我认为 Bloom Filter 适合用来做高吞吐场景的缓存穿透防护。请帮我检验这个理解。
```

```text
我觉得小模型微调对 Emacs Lisp 这类小众语言可能比继续换大模型更划算。请帮我拆解前提和风险。
```

### 不太适合的输入

```text
讲讲数据库。
```

太宽泛。建议改成：

```text
为什么 PostgreSQL 的 MVCC 需要 vacuum？它解决了什么历史版本回收问题？
```

```text
帮我学操作系统。
```

太大。建议改成：

```text
进程、线程、协程的调度边界分别在哪里？请用 Linux 视角解释。
```

### 新建选项

- 类型：`问题` 或 `观点`。
- 联网搜索：适合新发布模型、新闻、版本差异等时效性问题，需要先在设置中配置 Tavily API Key。
- 知识节点：把 session 绑定到知识树节点，用于长期统计掌握度。

提交后 session 会进入 `preparing`，后台生成标题和学习材料。生成完成后状态变成 `learning`。

## 4. 阅读学习材料

AI 初始材料通常包括概念解释、关键机制、例子和注意点。阅读时建议做三件事：

1. 先看结构，不急着记细节。
2. 标出自己说不清的术语。
3. 读完马上进入“写理解”，不要只停留在看。

AIIterate 的价值不在第一段 AI 回答，而在后面的主动复述、评价、修正和检验。

## 5. 深化阶段：写理解

“写理解”是最关键的一步。你要用自己的话解释刚学的内容。

好的写法：

```text
MVCC 通过给每行记录维护多个版本，让读事务根据自己的快照读取可见版本；写事务创建新版本而不是直接覆盖旧版本。这样读不会阻塞写，写也不会阻塞读。但两个事务更新同一行时仍然需要锁或冲突检测，否则写写冲突会破坏一致性。旧版本不能无限保留，所以 PostgreSQL 还需要 vacuum 回收不可见 tuple。
```

差的写法：

```text
MVCC 就是多版本，能提高并发。
```

为什么差：

- 没有解释“版本”怎么被读到。
- 没有说明读写冲突与写写冲突的区别。
- 没有提旧版本回收。
- 无法证明你真的理解了机制。

AI 评价会给出：

- 分数：0-100。
- 表扬：你说对了什么。
- gaps：薄弱点，会进入 gap ledger。
- verdict：是否建议进入下一步。
- suggested prompts：建议追问方向。

## 6. 深化阶段：追问

如果某个点不清楚，用“追问”。追问适合解决：

- 某个术语不懂。
- 某个机制的边界条件不清楚。
- 想要例子、反例或工程实践。
- AI 评价指出了 gap，但你不知道怎么补。

追问示例：

```text
你刚才说快照决定可见性。PostgreSQL 里 xmin/xmax 和事务 ID 是怎么参与判断的？
```

```text
能不能用一个订单创建 API 的例子解释幂等键如何防止重复下单？
```

```text
我还是没分清安全方法和幂等方法，能对比 GET、PUT、POST 吗？
```

追问回答会进入历史轮次，后续费曼检验会参考这些内容。

## 7. 费曼检验

至少写过一次理解后，才能开始费曼检验。

流程：

1. 点击开始费曼检验。
2. 系统生成 2 到 3 道检验题。
3. 逐题用自己的话回答。
4. 提交后 AI 逐题评分并生成总评。

高质量费曼答案通常包含：

- 核心机制。
- 具体例子。
- 边界条件。
- 常见误区。
- 和相似概念的区别。

示例：

```text
幂等性指同一个请求执行一次和执行多次的最终效果一致。GET 通常安全且幂等，因为它不应该改变服务器状态；PUT 通常幂等，因为多次把资源设置成同一个表示，最终状态相同；POST 默认不幂等，因为每次可能创建新资源。订单创建接口如果要支持重试，可以让客户端提供 idempotency_key，服务端对 user_id + key 建唯一约束，并把第一次执行结果持久化，后续相同 key 直接返回第一次结果。
```

低质量答案：

```text
幂等就是多次执行结果一样，接口要避免重复。
```

它不是完全错，但缺少机制、例子和工程细节，分数通常不会高。

## 8. 费曼结果怎么处理

### 通过

如果总分达到设置里的 `feynman_pass_score`：

- session 进入 `completed`。
- 保存 `review_report`。
- 自动创建复习计划。
- 知识树掌握度更新。

通过不是结束，只是进入长期记忆阶段。

### 未通过

如果总分低于通过分：

- session 进入 `revising`。
- 系统生成 correction plan。
- weak points 回流到 gap ledger。
- 你需要回到深化阶段继续写理解或追问。

不要把未通过看成失败。它的意义是暴露“以为懂了但说不清”的地方。

## 9. 复习

完成学习后，系统会按个性化艾宾浩斯间隔排期复习。

复习不是简单点击完成，而是重新解释概念：

```text
请不用看原文，重新解释这个主题。尽量说明机制、例子、边界和常见误解。
```

提交后 AI 会评价：

- 复习解释质量。
- 是否遗忘关键点。
- 下一轮复习间隔。

跳过复习只会标记 `skipped`，不算完成，也不会自动排下一轮。它适合临时没时间，而不是长期逃避。

## 10. 指挥中心

指挥中心是日常入口。它会把散落在系统里的学习事项聚合成优先级列表。

建议处理顺序：

1. 到期复习：防止长期记忆断档。
2. 待完成费曼：不要让已经开始的检验悬空。
3. 修正中 session：把失败费曼留下的 gaps 补掉。
4. 进行中 session：继续推进未完成学习。
5. 推荐节点：从知识树薄弱处继续学习。

如果系统健康区域出现异常，优先处理：

- pending/running jobs 堆积。
- error sessions。
- invariant 检查失败。
- parse_failed 明显增多。

## 11. 知识地图

知识地图用于长期追踪不同知识节点的掌握度。

节点状态含义：

- 未触及：没有相关 session。
- 学习中：有未完成 session。
- 待复习：已完成但有复习压力。
- 薄弱：低分、gaps 或逾期复习较多。
- 已掌握：完成率和分数较高，gap 压力低。

新建 session 时尽量绑定知识节点。未绑定也可以后续在 workspace 或知识地图里补绑。

绑定的好处：

- 掌握度统计更准确。
- 推荐下一步更有依据。
- 周报和复习压力更接近真实学习结构。

## 12. 设置

设置页包含几类配置。

### AI 配置

- provider：供应商标识，例如 `deepseek`。
- base_url：OpenAI-compatible API 地址。
- api_key：模型 API key，保存后只显示掩码。
- model：默认模型。
- roles：按任务类型覆盖模型。

### 联网搜索

Tavily API Key 用于联网搜索。适合：

- 新模型、新版本、新政策。
- 需要当前资料的问题。
- 文档、新闻、发布日期相关问题。

不需要最新信息的基础概念，可以关闭联网搜索，减少延迟。

### 学习设置

`feynman_pass_score` 是费曼通过分，默认建议 60。分数越高，系统越严格。

### 数据库

数据库配置影响整个系统。修改前建议先备份，避免切错库导致看不到原数据。

## 13. 状态说明

```text
preparing  正在准备：后台生成标题和学习材料
learning   初始材料已生成，可以开始学习
 deepening 正在深化：已经写理解或追问
feynman    费曼中：有待提交题组
revising   修正中：费曼未通过，需要补薄弱点
completed  已完成：已进入复习计划
error      异常：后台任务或 AI 调用失败
```

常见动作限制：

- 没写过理解，不能开始费曼。
- 已经在费曼中，不能重复创建新题组；系统会复用 pending group。
- 已完成或修正中的 session 可以 reopen。
- 状态不合法时会返回 `409 Conflict`。

## 14. 常见问题

### 页面提示未登录

重新输入 Admin Token。若仍失败，确认服务端设置里的 admin token 没有被清空，并检查浏览器是否禁用了 cookie。

### 新建后一直 preparing

说明后台 job 没有完成。服务端检查：

```bash
journalctl --user -u aiterate.service -f
```

也可以检查：

```text
/api/jobs/status
```

如果 job stuck，可让维护端执行 stale job 恢复或重启服务。

### AI 返回格式异常

系统会尽量从 LLM 输出中提取 JSON。解析失败会标记 `parse_failed`，不会静默当作成功。如果频繁失败：

- 检查当前模型是否适合严格 JSON 输出。
- 降低输出复杂度。
- 换更稳定的 evaluate/review 模型。
- 查看日志中的原始错误。

### 费曼分数为什么低

常见原因：

- 只写结论，没有机制。
- 没有例子。
- 没有说明边界条件。
- 把相似概念混在一起。
- 回答太短，无法证明理解。

改进方式：

```text
概念是什么 → 为什么需要它 → 它怎么工作 → 一个例子 → 它不适合什么情况 → 常见误解
```

### 复习太多怎么办

优先做逾期复习和低分节点。临时没时间可以跳过，但不要长期堆积。长期堆积说明新建 session 太多，完成闭环太少。

### 什么时候开联网搜索

适合：新技术、新版本、新闻、模型发布、当前价格、当前政策。基础概念不用开，速度更快、结果更稳定。

## 15. 推荐使用习惯

- 一个 session 只研究一个核心问题。
- 新建问题要具体，不要宽泛。
- 每次至少写一次“自己的理解”。
- 费曼答案写机制、例子、边界和误区。
- 每天优先看指挥中心。
- 学习主题尽量绑定知识节点。
- 复习时不要复制原文，强制自己重新解释。
- 如果费曼失败，先补 correction plan 里的薄弱点，再重测。

## 16. 一句话工作流

```text
每天打开指挥中心 → 先复习 → 补未通过费曼 → 推进一个学习中 session → 新建主题前先确认旧主题是否闭环。
```
