# AIIterate 用户使用文档

AIIterate 是一个把“提问 → 理解 → 复述 → 检验 → 复习”固定成闭环的个人学习系统。它不是普通聊天窗口，核心目标是帮助你确认自己是否真正掌握一个概念。

## 1. 登录与入口

打开：`http://192.168.31.222:7070`

如果出现登录页，输入 Admin Token。登录成功后浏览器会保存会话 cookie；后续刷新不需要重复登录。

顶部按钮：

- `＋`：新建学习主题。
- `知识地图`：查看知识树掌握度、绑定学习节点。
- `指挥中心`：查看今天最该推进的学习/复习事项。
- `设置`：配置模型、联网搜索、数据库、费曼通过分。
- `刷新`：重新拉取当前数据。
- `切换主题`：暗色/亮色主题切换。

## 2. 新建学习主题

点击 `＋`，输入一个问题或观点。

推荐写法：

- 问题：`PostgreSQL 的 MVCC 是怎么避免读写冲突的？`
- 观点：`我认为 Bloom Filter 适合用来做高吞吐场景的缓存穿透防护。请帮我检验这个理解。`

可选项：

- 类型：问题 / 观点。
- 联网搜索：用于需要最新信息的问题。联网搜索需要在设置里配置 Tavily API Key。
- 知识节点：可以选择知识树节点，方便长期统计掌握度。

提交后系统会创建一个 `preparing` session，后台生成标题和学习材料。完成后状态进入 `learning`。

## 3. 学习阶段

学习页展示：

- 原始问题。
- AI 生成的结构化学习材料。

先读材料，不要急着点完成。AIIterate 的价值在后面的“主动复述”和“费曼检验”。

## 4. 深化阶段

进入“深化”页后有两种操作：

### 写理解

用自己的话复述你对材料的理解。系统会评分并指出薄弱点。

好的写法：

```text
MVCC 通过给每行记录维护版本信息，让读事务根据快照读取自己可见的版本；写事务创建新版本而不是直接覆盖旧版本。这样读不会阻塞写，写也不会阻塞读。但更新同一行仍需要锁来保证写写冲突的一致性。
```

差的写法：

```text
MVCC 就是多版本，能提高并发。
```

### 追问

对不理解的点继续提问。追问回答会进入学习历史，后续费曼检验会参考这些内容。

## 5. 费曼检验

当至少写过一次理解后，可以开始费曼检验。

流程：

1. 点击开始复盘/费曼检验。
2. 系统生成若干道题。
3. 逐题用自己的话回答。
4. 提交后系统评分。

结果：

- 分数达到设置里的通过分：session 进入 `completed`，并自动排入复习计划。
- 分数未达标：session 进入 `revising`，系统给出修正计划和薄弱概念，回到深化阶段继续补足。

注意：费曼检验不是考试分数游戏。答案越具体、能举例、能说明边界条件，越能证明真正掌握。

## 6. 复习

完成学习后，系统会根据个性化艾宾浩斯间隔创建复习计划。

在指挥中心或 workspace 中可以看到待复习项。复习时重新解释这个概念，系统会给反馈并安排下一轮。

跳过复习只会标记 `skipped`，不会当作完成，也不会自动排下一轮。

## 7. 指挥中心

指挥中心按优先级展示：

- 必须做：逾期复习、待完成费曼。
- 推进中：正在学习、深化、修正中的主题。
- 未来 7 天复习：近期要复习的项目。
- 系统健康：后台任务、异常 session、解析失败等健康状态。

每天打开系统时，优先看指挥中心。

## 8. 知识地图

知识地图用于长期追踪不同知识节点的掌握度。

节点状态通常包括：

- 未触及：没有相关学习记录。
- 学习中：有未完成 session。
- 待复习：已学完但有复习压力。
- 已掌握：完成度和分数较高。
- 薄弱：低分、gap 或逾期复习较多。

新建 session 时尽量绑定节点，这样推荐和统计才准确。

## 9. 设置

设置页包含：

- AI 基础配置：provider、base_url、api_key、默认模型。
- 分角色模型：标题、回答、评价、复习、深化可以用不同模型。
- 联网搜索：Tavily API Key。
- 数据库：PostgreSQL/SQLite 配置测试与保存。
- 学习：费曼通过分。

API Key 保存后，页面只显示掩码，不返回明文。

## 10. 常见问题

### 页面提示未登录

重新输入 Admin Token 登录。若仍失败，确认服务端 `profile.settings.admin_token` 没有被清掉。

### 新建后一直 preparing

说明后台 job 没有完成。到服务端检查：

```bash
journalctl --user -u aiterate.service -f
```

也可以看 `/api/jobs/status` 或数据库 `jobs` 表。

### AI 返回格式异常

系统会尽量从 LLM 输出中提取 JSON；解析失败会标记 `parse_failed`，不会静默当作成功。遇到频繁失败时，优先检查模型是否遵守 JSON 输出要求。

### 为什么数据库被清空了？

本次项目级优化完成后按要求清除了历史学习数据，只保留设置和 admin token。清理前已做 PostgreSQL 备份，可从备份恢复。

## 11. 推荐使用习惯

- 一个 session 只研究一个核心问题。
- 每次至少写一次“自己的理解”，不要只读 AI 回答。
- 费曼答案写具体机制、例子和边界。
- 每天先看指挥中心。
- 学习主题尽量绑定知识节点。
